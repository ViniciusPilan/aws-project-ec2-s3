upstream web-server {
    server localhost; #Ip machine one
    server localhost; #Ip machine two
}

server {
    listen       80;
    server_name  localhost;
    root /var/www/html;

    location ^~ / {
        proxy_pass http://web-server/;
    }
}
